package interfaces;

public interface OpenInterface {
	
	// This function should take a path of images and save them in a subfolder of that path
	// called images. The images should be saved in a jpg format. 
	public abstract void openFunction(String path);

}
